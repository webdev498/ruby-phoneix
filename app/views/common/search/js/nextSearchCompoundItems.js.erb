$("#compoundItem_search_modal_partial").html(
		"<%= escape_javascript(render partial: 'common/search/compound_item_search_modal_template', locals: { searchItems: @searchItems, searchController: @searchController } ) %>");
