$("#claim_search_modal_partial").html(
    "<%= escape_javascript(render partial: 'common/search/claim_search_modal_template', locals: { searchClaims: @searchClaims, searchController: @searchController } ) %>");
